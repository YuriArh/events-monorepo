{
  "name": "api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc",
    "start": "node --env-file=.env dist/server.js",
    "lint": "biome lint .",
    "check-types": "tsc --noEmit",
    "test": "pnpm test:db:migrate && vitest run",
    "test:watch": "vitest",
    "test:db:migrate": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/eventapp_test pnpm --filter @repo/db exec prisma migrate deploy"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/multipart": "^10.1.1",
    "@fastify/static": "^10.1.4",
    "@repo/db": "workspace:*",
    "fastify": "^5.12.5",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "26.4.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
